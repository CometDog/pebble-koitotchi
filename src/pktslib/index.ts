import { sendAppMessage } from "./appMessage";
import { fetchString, fetchJSON, fetchBinary } from "./fetch";
import {
  createLogger,
  initDebugMode,
  setDebugMode,
  isDebugMode,
} from "./logger";

export {
  sendAppMessage,
  fetchString,
  fetchJSON,
  fetchBinary,
  createLogger,
  initDebugMode,
  setDebugMode,
  isDebugMode,
};

export default {
  sendAppMessage,
  fetchString,
  fetchJSON,
  fetchBinary,
  createLogger,
  initDebugMode,
  setDebugMode,
  isDebugMode,
};
